{
  "name": "@aigc/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vue-tsc -b --noEmit && vite build",
    "lint": "vue-tsc -b --noEmit",
    "test": "vue-tsc -b --noEmit",
    "typecheck": "vue-tsc -b --noEmit"
  },
  "dependencies": {
    "@aigc/shared-contracts": "workspace:*",
    "@vitejs/plugin-vue": "^5.2.3",
    "pinia": "^3.0.2",
    "vite": "^6.3.5",
    "vue": "^3.5.13",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vue-tsc": "^2.2.10"
  }
}
